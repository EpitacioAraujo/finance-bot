{
  "scripts": {
    "start": "docker run --rm -p 3000:3000 finance-bot"
  },
  "steps": {
    "build": {
      "commands": [
        "docker build -f devops/docker/Dockerfile.app -t finance-bot ."
      ]
    }
  }
}
